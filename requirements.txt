# Python 3.11 通用运行依赖（默认 CPU 版 PyTorch）
fastapi==0.139.0
redis==5.2.1
uvicorn[standard]==0.51.0
python-multipart==0.0.32

numpy==2.3.5
Pillow==12.3.0

# 支持 GPU 加速请在安装本文件后执行：python -m accelerator.install --backend auto
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.7.1+cpu
torchvision==0.22.1+cpu
